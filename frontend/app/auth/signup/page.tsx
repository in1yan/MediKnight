import { SignupForm } from '@/components/auth/signup-form';

export const metadata = {
  title: 'Sign Up - Healthcare Portal',
  description: 'Create a new healthcare portal account',
};

export default function SignupPage() {
  return <SignupForm />;
}
