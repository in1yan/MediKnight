import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Login - Healthcare Portal',
  description: 'Sign in to your healthcare portal',
};

export default function LoginPage() {
  return <LoginForm />;
}
